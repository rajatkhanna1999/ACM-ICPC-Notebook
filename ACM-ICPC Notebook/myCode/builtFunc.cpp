//Some Inbuilt Containers:
vector<char> v;
map<key, value> map;
unordered_map<key,value> umap;
queue<int> q;
priority_queue<int> pq;
pair<int,int> p1;
set<int> s;
multiset<int> s;
stack<int> stk;
list<int> lst;
deque<int> dq;
vector<int>::iterator it;
//v.insert(),v.push_back(),v.pop_back,v.erase()

//Inbuilt Algorithms
__builtin_popcount (n);
binary_search(first,last,value);
lower_bound(first,last,value);
upper_bound(first,last,value);
find(first,last,value);
max(a,b),min(a,b),swap(a,b);
sort(first,last),sort(arr,arr+n);
next_permutations(v.begin(),v.end())//(first,last)
reverse(first,last);
rotate(first,last);
